<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RetailBuddy - Login</title>
  
  <!-- Favicon -->
  <%= favicon_link_tag "retail_icon.png?v=#{Rails.application.config.assets.version}", type: 'image/png' %>
  
  <!-- Rails asset pipeline for CSS -->
  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "custom", media: "all", "data-turbo-track": "reload" %>
  
  <!-- Font Awesome & Google Fonts -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <div id="component-container">
    <div class="background-shapes">
      <div class="shape"></div>
      <div class="shape"></div>
      <div class="shape"></div>
      <div class="shape"></div>
    </div>
    <div class="login-container">
      <div class="login-image">
        <!-- Replace with your asset or proper path -->
        <img id="login-hero-image" src="<%= asset_path 'login-hero.jpg' %>" alt="Retail management system illustration">
      </div>
      <div class="login-form" data-controller="theme">
        <!-- Dark mode toggle inside the container (handled by Stimulus) -->
        <button class="theme-toggle" id="themeToggle" data-action="click->theme#toggle">
          <i class="fas fa-moon"></i>
        </button>
        <div class="logo-container">
          <div class="logo">
            <i class="fas fa-shopping-bag"></i>
            <span>RetailBuddy</span>
          </div>
        </div>
        <h1>Welcome Back</h1>
        <p class="welcome-text">Enter your credentials to access your account</p>
        
        <!-- Display flash messages -->
        <div id="flash-messages">
          <% if flash[:alert] %>
            <p class="alert"><%= flash[:alert] %></p>
          <% elsif flash[:notice] %>
            <p class="notice"><%= flash[:notice] %></p>
          <% end %>
        </div>
          <% flash.each do |type, message| %>
          <div class="alert alert-<%= type == 'notice' ? 'success' : 'danger' %>">
          <%= message %>
          </div>
        <% end %>
        
        <!-- Login form using form_with -->
        <%= form_with url: login_path, method: :post, local: true, html: { class: "login-form" } do |f| %>
          <div class="input-group">
            <label for="employee_id">Employee ID</label>
            <div class="input-field">
              <i class="fas fa-envelope"></i>
              <%= f.text_field :user_id, id: "employee_id", placeholder: "Employee ID", autocomplete: "off", required: true %>
            </div>
          </div>
          <div class="input-group">
            <label for="password">Password</label>
            <div class="input-field">
              <i class="fas fa-lock"></i>
              <%= f.password_field :password, id: "password", placeholder: "Enter your password", autocomplete: "current-password", required: true %>
            </div>
          </div>
          <!-- Login Buton -->
          <div class="button-container">
            <button type="submit" class="login-button">
              Login to Dashboard
            </button>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  
  <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
</body>
</html>
